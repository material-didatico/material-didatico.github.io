-- frame:4: -------------------------------------------------------------------

import sympy as sp

sp.init_printing()


-- frame:4: -------------------------------------------------------------------

import sympy as sp

sp.init_printing()


-- frame:4: -------------------------------------------------------------------

import sympy as sp

sp.init_printing()


-- frame:6: -------------------------------------------------------------------

x, y, t, a = sp.symbols('x y t alpha')


-- frame:6: -------------------------------------------------------------------

p = x**3 + 2*x**2 - 3*x


-- frame:6: -------------------------------------------------------------------

x, y, t, a = sp.symbols('x y t alpha')


-- frame:6: -------------------------------------------------------------------

p = x**3 + 2*x**2 - 3*x


-- frame:6: -------------------------------------------------------------------

x, y, t, a = sp.symbols('x y t alpha')


-- frame:6: -------------------------------------------------------------------

p = x**3 + 2*x**2 - 3*x


-- frame:6: -------------------------------------------------------------------

x, y, t, a = sp.symbols('x y t alpha')


-- frame:6: -------------------------------------------------------------------

p = x**3 + 2*x**2 - 3*x


-- frame:6: -------------------------------------------------------------------

x, y, t, a = sp.symbols('x y t alpha')


-- frame:6: -------------------------------------------------------------------

p = x**3 + 2*x**2 - 3*x


-- frame:6: -------------------------------------------------------------------

x, y, t, a = sp.symbols('x y t alpha')


-- frame:6: -------------------------------------------------------------------

p = x**3 + 2*x**2 - 3*x


-- frame:6: -------------------------------------------------------------------

x, y, t, a = sp.symbols('x y t alpha')


-- frame:6: -------------------------------------------------------------------

p = x**3 + 2*x**2 - 3*x


-- frame:7: -------------------------------------------------------------------

print(x)
print(y)
print(t)
print(a)
print(p)


-- frame:7: -------------------------------------------------------------------

sp.pprint(x)
sp.pprint(y)
sp.pprint(t)
sp.pprint(a)
sp.pprint(p)


-- frame:7: -------------------------------------------------------------------

print(x)
print(y)
print(t)
print(a)
print(p)


-- frame:7: -------------------------------------------------------------------

sp.pprint(x)
sp.pprint(y)
sp.pprint(t)
sp.pprint(a)
sp.pprint(p)


-- frame:9: -------------------------------------------------------------------

sp.factor(p)


-- frame:9: -------------------------------------------------------------------

sp.factor(p)


-- frame:10: -------------------------------------------------------------------

sp.expand((x + 2)*(x - 3))


-- frame:10: -------------------------------------------------------------------

sp.expand((x + 2)*(x - 3))


-- frame:11: -------------------------------------------------------------------

sp.simplify((x**2 - 1)/(x - 1))


-- frame:11: -------------------------------------------------------------------

sp.simplify((x**2 - 1)/(x - 1))


-- frame:12: -------------------------------------------------------------------

sp.diff(x**4 - 3*x**2 + 5, x)


-- frame:12: -------------------------------------------------------------------

sp.diff(x**4 - 3*x**2 + 5, x)


-- frame:13: -------------------------------------------------------------------

sp.integrate(p, x)


-- frame:13: -------------------------------------------------------------------

sp.integrate(p, x)


-- frame:14: -------------------------------------------------------------------

(x**2 + 3*x + 1).subs(x, t + 1)


-- frame:14: -------------------------------------------------------------------

(x**2 + 3*x + 1).subs(x, t + 1)


-- frame:14: -------------------------------------------------------------------

(x**2 + 3*x + 1).subs(x, t + 1)


-- frame:15: -------------------------------------------------------------------

sp.expand((x**2 + 3*x + 1).subs(x, t + 1))


-- frame:15: -------------------------------------------------------------------

sp.expand((x**2 + 3*x + 1).subs(x, t + 1))


-- frame:16: -------------------------------------------------------------------

p.subs(x, 2)


-- frame:16: -------------------------------------------------------------------

p.subs(x, 2)


-- frame:17: -------------------------------------------------------------------

sp.solve(x**3 - 1, x)


-- frame:17: -------------------------------------------------------------------

sp.solve(x**3 - 1, x)


-- frame:17: -------------------------------------------------------------------

sp.solve(x**3 - 1, x)


-- frame:17: -------------------------------------------------------------------

sp.solve(x**3 - 1, x)


-- frame:19: -------------------------------------------------------------------

sol = sp.solve([2*x + 3*y - 5,
                x - y - 1],
                [x, y])
sol


-- frame:19: -------------------------------------------------------------------

sol = sp.solve([2*x + 3*y - 5,
                x - y - 1],
                [x, y])
sol


-- frame:19: -------------------------------------------------------------------

sol = sp.solve([2*x + 3*y - 5,
                x - y - 1],
                [x, y])
sol


-- frame:19: -------------------------------------------------------------------

sol = sp.solve([2*x + 3*y - 5,
                x - y - 1],
                [x, y])
sol


-- frame:20: -------------------------------------------------------------------

A = sp.Matrix([[1, 2], [3, 4]])
b = sp.Matrix([5, 6])
r = A.LUsolve(b)
r


-- frame:20: -------------------------------------------------------------------

A = sp.Matrix([[1, 2], [3, 4]])
b = sp.Matrix([5, 6])
r = A.LUsolve(b)
r


-- frame:20: -------------------------------------------------------------------

A = sp.Matrix([[1, 2], [3, 4]])
b = sp.Matrix([5, 6])
r = A.LUsolve(b)
r


-- frame:20: -------------------------------------------------------------------

A = sp.Matrix([[1, 2], [3, 4]])
b = sp.Matrix([5, 6])
r = A.LUsolve(b)
r


-- frame:21: -------------------------------------------------------------------

a, b = sp.symbols('alpha beta')
A = sp.Matrix([[a, 1], [1, b]])
b = sp.Matrix([1, 1])
r = A.LUsolve(b)
sp.simplify(r)


-- frame:21: -------------------------------------------------------------------

a, b = sp.symbols('alpha beta')
A = sp.Matrix([[a, 1], [1, b]])
b = sp.Matrix([1, 1])
r = A.LUsolve(b)
sp.simplify(r)


-- frame:21: -------------------------------------------------------------------

a, b = sp.symbols('alpha beta')
A = sp.Matrix([[a, 1], [1, b]])
b = sp.Matrix([1, 1])
r = A.LUsolve(b)
sp.simplify(r)


-- frame:22: -------------------------------------------------------------------

sol = sp.solve([x*y - 2,
                x**2 + y - 3],
               [x, y])
sol


-- frame:22: -------------------------------------------------------------------

sol = sp.solve([x*y - 2,
                x**2 + y - 3],
               [x, y])
sol


-- frame:22: -------------------------------------------------------------------

sol = sp.solve([x*y - 2,
                x**2 + y - 3],
               [x, y])
sol


-- frame:23: -------------------------------------------------------------------

sol = sp.nsolve([sp.sin(x) + y - 1,
                 x + sp.cos(y) - 1],
                 [x, y],
                 [0.5, 0.5])
sol


-- frame:23: -------------------------------------------------------------------

sol = sp.nsolve([sp.sin(x) + y - 1,
                 x + sp.cos(y) - 1],
                 [x, y],
                 [0.5, 0.5])
sol


-- frame:23: -------------------------------------------------------------------

sol = sp.nsolve([sp.sin(x) + y - 1,
                 x + sp.cos(y) - 1],
                 [x, y],
                 [0.5, 0.5])
sol


